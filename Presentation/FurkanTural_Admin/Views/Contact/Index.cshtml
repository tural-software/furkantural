@using FurkanTural_Admin.Models.Contact
@model ContactIndexViewModel

@section Styles {
    <link rel="stylesheet" href="~/css/pages/contact/contact-index.css" asp-append-version="true" />
}

@section Scripts {
    <script>
        window.__contactMeta = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(new {
            name          = Model.SearchName ?? "",
            activeFilter  = Model.ActiveFilter ?? "",
            deletedFilter = Model.DeletedFilter ?? "",
            readFilter    = Model.ReadFilter ?? "",
            dateFrom      = Model.DateFrom ?? "",
            dateTo        = Model.DateTo ?? "",
            pageNumber    = Model.PageNumber,
            pageSize      = Model.PageSize
        }));
    </script>
    <script src="~/js/detail-modal.js" asp-append-version="true"></script>
    <script src="~/js/confirm-modal.js" asp-append-version="true"></script>
    <script src="~/js/pages/contact-index.js" asp-append-version="true"></script>
}

@Html.AntiForgeryToken()

<div class="container">

    @await Html.PartialAsync("_Breadcrumb", "")

    <div class="pg-header">
        <div>
            @await Html.PartialAsync("_PageTitle")
            <p class="section-desc">Portfolyo iletişim formu üzerinden gönderilen mesajları görüntüleyin ve yönetin.</p>
        </div>
        <div class="pg-header__actions">
            <a href="@Url.Action("Index")" class="btn-outline">
                @await Html.PartialAsync("_Icons", "refresh")
                Yenile
            </a>
            <a href="@Url.Action("TableDetail")" class="btn-outline">
                @await Html.PartialAsync("_Icons", "table-icon")
                Tablo Detayları
            </a>
        </div>
    </div>

    <div class="stats-row">
        <div class="card stat-box">
            <div class="stat-box__icon stat-box__icon--total">
                @await Html.PartialAsync("_Icons", "mail")
            </div>
            <div>
                <p class="stat-box__label">Toplam Mesaj</p>
                <div class="stat-box__value">@Model.TotalCount.ToString("N0")</div>
                <span class="stat-box__unit">kayıt</span>
            </div>
        </div>
        <div class="card stat-box">
            <div class="stat-box__icon stat-box__icon--active">
                @await Html.PartialAsync("_Icons", "check-circle")
            </div>
            <div>
                <p class="stat-box__label">Aktif Kayıt</p>
                <div class="stat-box__value">@Model.ActiveCount.ToString("N0")</div>
                <span class="stat-box__unit">kayıt</span>
            </div>
        </div>
        <div class="card stat-box">
            <div class="stat-box__icon stat-box__icon--passive">
                @await Html.PartialAsync("_Icons", "mail-unread")
            </div>
            <div>
                <p class="stat-box__label">Okunmamış</p>
                <div class="stat-box__value">@Model.UnreadCount.ToString("N0")</div>
                <span class="stat-box__unit">kayıt</span>
            </div>
        </div>
        <div class="card stat-box">
            <div class="stat-box__icon stat-box__icon--deleted">
                @await Html.PartialAsync("_Icons", "trash")
            </div>
            <div>
                <p class="stat-box__label">Silinmiş Kayıt</p>
                <div class="stat-box__value">@Model.DeletedCount.ToString("N0")</div>
                <span class="stat-box__unit">kayıt</span>
            </div>
        </div>
    </div>

    <div class="card" style="padding: 1.25rem; margin-bottom: 1.25rem;">
        <form method="get" action="@Url.Action("Index")">
            <div class="filter-bar">
                <div class="filter-group">
                    <label>Ad / E-posta ara</label>
                    <div class="filter-input-wrap">
                        @await Html.PartialAsync("_Icons", "search")
                        <input type="text" name="name" value="@Model.SearchName"
                               class="filter-input filter-input--search"
                               placeholder="Ad veya e-posta ara..." />
                    </div>
                </div>
                <div class="filter-group">
                    <label>Okunma Durumu</label>
                    <select name="readFilter" class="filter-select">
                        <option value="" selected="@(string.IsNullOrEmpty(Model.ReadFilter))">Tümü</option>
                        <option value="read" selected="@(Model.ReadFilter == "read")">Okundu</option>
                        <option value="unread" selected="@(Model.ReadFilter == "unread")">Okunmadı</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label>Aktiflik Durumu</label>
                    <select name="activeFilter" class="filter-select">
                        <option value="" selected="@(string.IsNullOrEmpty(Model.ActiveFilter))">Tümü</option>
                        <option value="active" selected="@(Model.ActiveFilter == "active")">Aktif</option>
                        <option value="passive" selected="@(Model.ActiveFilter == "passive")">Pasif</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label>Silinme Durumu</label>
                    <select name="deletedFilter" class="filter-select">
                        <option value="" selected="@(string.IsNullOrEmpty(Model.DeletedFilter))">Tümü</option>
                        <option value="notDeleted" selected="@(Model.DeletedFilter == "notDeleted")">Silinmemiş</option>
                        <option value="deleted" selected="@(Model.DeletedFilter == "deleted")">Silinmiş</option>
                    </select>
                </div>
                <div class="filter-group">
                    <label>Başlangıç Tarihi</label>
                    <input type="date" name="dateFrom" value="@Model.DateFrom" class="filter-input" />
                </div>
                <div class="filter-group">
                    <label>Bitiş Tarihi</label>
                    <input type="date" name="dateTo" value="@Model.DateTo" class="filter-input" />
                </div>
                <div class="filter-group filter-group--actions">
                    <button type="submit" class="btn-primary">
                        @await Html.PartialAsync("_Icons", "filter-icon")
                        Filtrele
                    </button>
                    <a href="@Url.Action("Index")" class="btn-outline">@await Html.PartialAsync("_Icons", "x-mark") Temizle</a>
                </div>
            </div>
        </form>
    </div>

    <div class="card" id="contact-table-card" style="padding: 0; overflow: hidden;">
        <div id="contact-table-section">
            @await Html.PartialAsync("_ContactTable", Model)
        </div>
    </div>

</div>
